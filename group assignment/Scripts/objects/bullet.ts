module objects {
    export class  bullet  {
        // Variables
        // Constructor
        constructor(boolean) {
                /*
            this.regX = this.getBounds().width * 0.5;
            this.regY = this.getBounds().height * 0.5;

            this.x = x;
            this.y = y;
            */

        }
    }
}