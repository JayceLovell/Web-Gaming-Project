var objects;
(function (objects) {
    var bullet = /** @class */ (function () {
        // Variables
        // Constructor
        function bullet(boolean) {
            /*
        this.regX = this.getBounds().width * 0.5;
        this.regY = this.getBounds().height * 0.5;

        this.x = x;
        this.y = y;
        */
        }
        return bullet;
    }());
    objects.bullet = bullet;
})(objects || (objects = {}));
//# sourceMappingURL=bullet.js.map